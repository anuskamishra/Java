package OneToMany;

public class User {  // Changed 'user' to 'User'
    String username;  // Changed 'Username' to 'username' (camelCase)

    public User(String username) {  // Changed parameter to lowercase 'username'
        this.username = username;
    }
}
